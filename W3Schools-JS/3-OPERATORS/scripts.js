//O sinal de 1 igual (=) no JavaScript significa atribuição

let x = 10; // x recebe 10
let y = 2;
let z = x + y; // z recebe x + y (10 + 2)

document.write(`A soma de ${x} + ${y} = ${z}`);

//OBS: tomar cuidado ao usar o operador "+" pois ele também é usado para concatenação


//Operadores Aritméticos

 /* + SOMA
 - SUBTRAÇÃO
 / DIVISÃO
 * MULTIPLICAÇÃO
 % RESTO DA DIVISÃO
 ** EXPONENCIAÇÃO
 ++ INCREMENTO
 -- DECREMENTO 
 */


 //Operadores de atribuição - usa-se de acordo com a situação

    /*
    
    =	    x = y	    x = y
    +=	    x += y	    x = x + y
    -=	    x -= y	    x = x - y
    *=	    x *= y	    x = x * y
    /=	    x /= y	    x = x / y
    %=	    x %= y	    x = x % y
    **=	    x **= y	    x = x ** y 
    
    */


//Operadores de comparação

    /* 

    ==	igual
    ===	valor igual e tipo igual
    !=	diferente
    !==	valor diferente e tipo diferente
    >	maior
    <	menor
    >=	maior igual
    <=	menor igual
    ?	ternário 

    */

//Operadores lógicos

    /* 
    && and
    || or
    ! not 
    */
